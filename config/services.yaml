parameters:
    app.data_dir: '%kernel.project_dir%/var/data'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Service\SinistreService:
        arguments:
            $dataDir: '%app.data_dir%'

